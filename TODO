☐ Update README
☐ Change Modeltype to modelBuilder

☐ Maybe because we are now creating models that are dependent on the form (they are created with that data)
--> therefore when we are in edit, do not display the form selector (user cannot change the form they used to create that model)

☐ Allow for this package to act as a form as well???? So that the values will state form_x instead of model_type_x

☐ Set better options on field types
--> like precision on field